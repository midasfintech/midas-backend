meta {
  name: log-in
  type: http
  seq: 2
}

post {
  url: {{SUPABASE_URL}}/auth/v1/token?grant_type=password
  body: json
  auth: inherit
}

params:query {
  grant_type: password
}

body:json {
  {
    "email": "hello@timrekelj.si",
    "password": "Password123!"
  }
  
}

script:post-response {
  bru.setEnvVar("AUTH_TOKEN", res.body.access_token);
}
